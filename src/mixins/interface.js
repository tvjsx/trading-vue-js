
// Html interface, shown on top of the grid.
// Can be static (a tooltip) or interactive,
// e.g. a control panel.

export default {
    props: ['ux', 'updater'],
    methods: {

    },
    computed: {

    },
    data() {
        return {

        }
    }
}
